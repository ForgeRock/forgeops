apiVersion: batch/v1
kind: Job
metadata:
  name: config-util
spec:
  template:
    spec:
      containers:
      - name: config-util
        env:
        - name: CONFIG_FOLDER
          value: configs/amcdm