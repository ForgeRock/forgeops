apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "identity-platform.fullname" . }}-test-connection"
  labels:
    {{- include "identity-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "identity-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
