{{- if .Values.am.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: am-entrypoint
  labels:
    {{- include "identity-platform.labels" . | nindent 4 }}
data:
  {{ (.Files.Glob "files/am/entrypoint-scripts/entrypoint.sh").AsConfig | nindent 2 }}
{{- end }}