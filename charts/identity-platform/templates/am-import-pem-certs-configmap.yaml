{{- if .Values.am.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: am-import-pem-certs
  labels:
    {{- include "identity-platform.labels" . | nindent 4 }}
data:
  {{ (.Files.Glob "files/am/import-pem-certs.sh").AsConfig | nindent 2 }}
{{- end }}
