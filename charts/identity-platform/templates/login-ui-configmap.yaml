{{- if .Values.login_ui.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: login-ui-init-scripts
  labels:
    {{- include "identity-platform.labels" . | nindent 4 }}
    {{- with .Values.login_ui.podLabels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
data:
  {{ (.Files.Glob "files/login-ui/init-scripts/**.sh").AsConfig | nindent 2 }}
{{- end }}
