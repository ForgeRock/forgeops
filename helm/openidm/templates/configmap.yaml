apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "fullname" . }}
data:
  PROJECT_HOME: "/git/{{ .Values.git.configPath.idm }}"
  GIT_AUTOSAVE_BRANCH:  autosave-idm
  # These are now set in the bootstrap chart - as they are shared
#  GIT_REPO: "{{ .Values.git.repo }}"
#  GIT_BRANCH: {{ default "master" .Values.git.branch }}
#  GIT_ROOT:  "/git"

  

