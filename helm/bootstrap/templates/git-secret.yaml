# Note that secret values are base64-encoded.
{{ if not (hasPrefix "ssh:" .Values.global.git.repo) }}
apiVersion: v1
kind: Secret
metadata:
    name: "git-credentials"
type: Opaque
data:
{{ (.Files.Glob "secrets/*").AsSecrets| indent 2 }}
{{ end }}