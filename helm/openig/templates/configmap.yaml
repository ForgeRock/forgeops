apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "fullname" . }}
data:
  OPENIG_BASE: "/git/{{ .Values.global.git.configPath.ig }}"
  GIT_REPO: "{{ .Values.global.git.repo }}"
  GIT_BRANCH: {{ default "master" .Values.global.git.branch }}
