amster:
  domain: .example.com

openam:
  domain: .example.com

openig:
  domain: .example.com

openidm:
  domain: .example.com

userstore:
  instance: userstore
  persistence: false
  opendjJavaArgs: "-Xmx400m"
  resources:
    limits:
      memory: 600Mi
    requests:
      memory: 400Mi

configstore:
  instance: configstore
  persistence: false
  opendjJavaArgs: "-Xmx400m"
  resources:
    limits:
      memory: 600Mi
    requests:
      memory: 400Mi