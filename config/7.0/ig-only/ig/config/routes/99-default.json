{
  "handler": {
    "type": "StaticResponseHandler",
    "config": {
      "status": 200,
      "headers": {
        "Cache-Control": [
          "none"
        ],
        "Content-Type": [
          "text/plain"
        ],
        "X-Content-Type-Options": [
          "nosniff"
        ]
      },
      "entity": "hello and Welcome to OpenIG. Your path is ${request.uri.path}. OpenIG is using the default handler for this route with fix-up header"
    },
    "monitor": {
      "enabled": true
    }
  }
}
