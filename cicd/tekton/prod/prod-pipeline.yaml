apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: prod-pipeline
spec:
  resources:
    - name: forgeops
      type: git
  tasks:
  - name: skaffold-prod-deploy
    taskRef:
      name: skaffold-prod-deploy
    resources:
      inputs:
      - name: forgeops
        resource: forgeops

  # TODO: add tasks for smoke test, creating users, etc.