config:
  # git container does the checkout under /git/config so we have config twice
  path: /git/config/config/7.0/cdk/idm/
openidm:
  repo:
    host: userstore-0.userstore
    port: 1389
    user: "uid=admin"
    password: password
replicaCount: 2
service:
  name: idm
