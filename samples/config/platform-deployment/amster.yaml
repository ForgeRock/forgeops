config:
  claim: frconfig
  # git container does the checkout under /git/config so we have config twice
  importPath: /git/config/config/7.0/cdk/amster
serverBase: http://am:80
amadminPassword: "password"
configStore:
  host: userstore-0.userstore
userStore:
  dirManager: "uid=admin"
  storeType: "LDAPv3ForForgeRockIAM"
