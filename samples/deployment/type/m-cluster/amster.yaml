config:
  claim: frconfig
  importPath: /git/config/default/gatling/forgeops-benchmark-am
  exportPath: /tmp/amster


# For production set CPU limits to help Kube Schedule the pods.
resources:
 limits:
   memory: 756Mi
 requests:
   memory: 756Mi
