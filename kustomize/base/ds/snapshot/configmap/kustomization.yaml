apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ds-snapshot-configmap.yaml
labels:
- includeSelectors: true
  pairs:
    app: ds-snapshot
    app.kubernetes.io/name: ds-snapshot
    app.kubernetes.io/part-of: identity-platform
