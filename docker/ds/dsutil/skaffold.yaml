apiVersion: skaffold/v1beta14
kind: Config
build:
  artifacts:
  - image: dsutil
deploy:
  kubectl:
    manifests:
    - k8s/deployment.yaml
profiles:
- name: create-users
  deploy:
    kubectl:
      manifests:
      - k8s/create-job.yaml
- name: ds-bench
  deploy:
    kubectl:
      manifests:
      - k8s/bench-job.yaml
