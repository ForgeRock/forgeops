{
  "handler": {
    "type": "StaticResponseHandler",
    "config" : {
      "status": 200,
      "entity": "Welcome to OpenIG. Your path is ${request.uri.path}. OpenIG is using the default handler for this route."
    }
  }
}
